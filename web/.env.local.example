# Optional: connect a Supabase project for cross-device sync + shared database.
# Leave blank to run fully local (data stored in the browser).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
