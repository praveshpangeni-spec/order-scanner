# Optional: connect a Supabase project for cross-device sync + shared database.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only (do NOT prefix with NEXT_PUBLIC). Google Cloud Vision API key for OCR.
GOOGLE_VISION_API_KEY=
